# CarbonOps Assistant — Codex Cloud Run 02 — 2026-04-30

## Purpose

This run moves CarbonOps Assistant from a repository foundation into a small, deterministic assistant baseline.

The goal is not to build a real LLM product yet. The goal is to add testable local behavior around:

- sample question evaluation
- deterministic assistant orchestration
- small local knowledge-base loading/search
- documentation alignment with already implemented code
- conservative GTV-friendly evidence structure

## Current Repository Context

The repository already contains:

- Python package baseline
- local CLI baseline
- response contract
- deterministic guardrails
- emissions calculation helpers
- unit normalization helpers
- sample questions
- evaluation plan placeholder
- test-only GitHub Actions workflow

Do not duplicate these foundations. Extend them narrowly.

## Global Constraints

### Allowed

- Add small deterministic Python modules.
- Add unit tests.
- Add small JSON/Markdown example files.
- Improve documentation to match actual implementation.
- Add local evaluation runner functionality.
- Add a tiny local knowledge-base loader/search baseline.
- Keep dependencies minimal.
- Use only standard library unless a dependency is clearly justified.

### Forbidden

Do not:

- add LLM SDK dependencies
- call external APIs
- add deploy or publish automation
- add PyPI publishing
- add Docker registry workflows
- add cloud infrastructure
- add authentication or multi-tenancy
- add real secrets, tokens, or API keys
- claim production readiness
- claim regulatory compliance
- claim certification
- claim complete emissions accounting coverage
- create a complex plugin/provider architecture
- add a web UI

## Branch and PR Rules for Codex Cloud

Create a new working branch. Do not commit directly to `main`.

Suggested branch name:

```text
codex/cloud-run-02-evaluation-baseline
```

Open a pull request when done.

Suggested PR title:

```text
COA Cloud Run 02 — Add deterministic evaluation and knowledge baseline
```

## Validation Rules

After each task:

```bash
python -m pytest -q
git status
```

If tests fail:

1. Fix only if the failure is clearly caused by the current task.
2. Re-run tests.
3. If still failing, stop and report.
4. Do not continue to the next task.

Before final PR:

```bash
python -m pytest -q
grep -RniE "production-ready|enterprise-grade|certified|compliant|fully automated|regulatory approved|docker login|pypi|secret|token|password" README.md docs .github src tests examples pyproject.toml || true
git status
git log --oneline -10
```

Risky language is acceptable only when used in non-goals, disclaimers, or forbidden-scope documentation.

## Commit Rules

Use small commits. Do not squash.

Suggested commits:

```text
COA-007 Add deterministic evaluation runner
COA-008 Add local knowledge-base loader and search baseline
COA-009 Add deterministic assistant orchestration baseline
COA-010 Align documentation with implemented baseline
COA-011 Add evidence backlog for next development phase
```

Do not commit broken tests.

---

# Task Queue

## COA-007 — Deterministic Evaluation Runner

### Objective

Turn `examples/sample_questions.json` from a passive example file into something that can be validated locally.

### Required Changes

Create:

```text
src/carbonops_assistant/evaluation/__init__.py
src/carbonops_assistant/evaluation/cases.py
src/carbonops_assistant/evaluation/runner.py
tests/test_evaluation_cases.py
tests/test_evaluation_runner.py
```

Update only if useful:

```text
examples/sample_questions.json
docs/evaluation/evaluation-plan.md
```

### Implementation Requirements

Implement:

- loading JSON evaluation cases from a file path
- validating required fields:
  - `id`
  - `question`
  - `expected_status`
- validating `expected_status` against response contract statuses
- running each question through the existing deterministic guardrail/response baseline
- returning a simple result object with:
  - case id
  - expected status
  - actual status
  - passed boolean
  - notes or reason

Keep the runner deterministic.

Do not call LLMs.

### Tests

Add tests for:

- valid case loading
- missing required field
- invalid expected status
- sample questions file can be loaded
- runner returns pass/fail results
- runner uses existing guardrail behavior

### Completion Criteria

- `python -m pytest -q` passes
- commit created

---

## COA-008 — Local Knowledge-Base Loader and Search Baseline

### Objective

Add a tiny local knowledge-base baseline for future assistant context. This is not vector search and not RAG yet.

### Required Changes

Create:

```text
src/carbonops_assistant/knowledge/__init__.py
src/carbonops_assistant/knowledge/documents.py
src/carbonops_assistant/knowledge/search.py
docs/knowledge/base.md
tests/test_knowledge_documents.py
tests/test_knowledge_search.py
```

### Implementation Requirements

Implement a narrow standard-library-only baseline:

- load Markdown documents from file path
- split document into simple sections by Markdown headings
- represent each section with:
  - id or slug
  - title
  - content
  - source path
- implement keyword-based search over title/content
- return ranked results using simple deterministic scoring

Do not add vector databases.
Do not add embedding dependencies.
Do not call external services.

### Knowledge Content Rules

`docs/knowledge/base.md` should include only conservative, high-level project-local knowledge such as:

- what the project currently supports
- what inputs deterministic emissions helpers need
- limitations and non-goals
- why assumptions matter

Do not include legal/regulatory advice.
Do not claim compliance.

### Tests

Add tests for:

- Markdown section loading
- stable section IDs/slugs
- search finds relevant sections
- search returns empty list for unrelated query
- ranking is deterministic

### Completion Criteria

- standard library only
- `python -m pytest -q` passes
- commit created

---

## COA-009 — Deterministic Assistant Orchestration Baseline

### Objective

Add a small local orchestrator that connects existing guardrails, emissions helper availability, and knowledge search.

This is still not an LLM assistant.

### Required Changes

Create:

```text
src/carbonops_assistant/assistant/orchestrator.py
tests/test_orchestrator.py
```

Update if useful:

```text
src/carbonops_assistant/cli.py
```

### Implementation Requirements

Implement an `answer_question` or similarly named function that:

1. accepts a question string
2. checks empty/risky/needs-more-context cases using existing guardrails
3. optionally searches the local knowledge base if provided
4. returns `AssistantResponse`
5. includes limitations for baseline behavior
6. includes sources when knowledge sections are used

Keep behavior deterministic.

Do not add LLM provider abstractions yet.
Do not add prompt templates yet unless they are static and clearly needed.

### CLI Scope

If updating CLI, keep it minimal:

- support status output
- optionally support a local `ask`-style command using deterministic orchestrator
- no external calls
- no API keys

### Tests

Add tests for:

- empty input
- risky compliance/certification wording
- vague calculation question
- knowledge-backed answer includes sources
- answer remains conservative when no knowledge base is provided

### Completion Criteria

- deterministic behavior
- no external calls
- `python -m pytest -q` passes
- commit created

---

## COA-010 — Documentation Alignment

### Objective

Update documentation so it accurately reflects implemented code after COA-007 to COA-009.

### Required Changes

Update:

```text
README.md
docs/development.md
docs/testing.md
docs/roadmap.md
docs/evaluation/evaluation-plan.md
```

Create if useful:

```text
docs/architecture.md
```

### Documentation Requirements

README should reflect implemented features accurately:

- Python package baseline
- deterministic response contract
- guardrails
- emissions calculation helpers
- local evaluation runner
- local knowledge baseline
- deterministic orchestrator if added

Clearly separate:

- implemented
- current limitations
- planned
- non-goals

Fix any stale wording such as saying deterministic helpers are only planned if they are already implemented.

### Completion Criteria

- no inflated claims
- no unsupported compliance/production wording
- `python -m pytest -q` passes
- commit created

---

## COA-011 — Evidence Backlog for Next Phase

### Objective

Add a realistic evidence-oriented backlog for the next development phase.

### Required Changes

Create:

```text
docs/evidence-backlog.md
```

### Content Requirements

Include:

- next 30-day technical tasks
- GTV evidence relevance
- risk level
- implementation notes
- non-goals
- candidate future GitHub issue titles

Focus on:

- evaluation quality
- documentation quality
- deterministic assistant behavior
- future Hugging Face dataset/model-card readiness
- small domain-specific examples

Do not claim this backlog is completed.

### Completion Criteria

- factual backlog
- no fake achievements
- `python -m pytest -q` passes
- commit created

---

## Final Report Required

At the end, report exactly:

```text
A. Summary
B. Files changed
C. Tests run and result
D. Safety scan result
E. Commits created with hashes
F. Pull request result
G. Remaining risks
H. Recommended next task
```

# CarbonOps Assistant — Codex Cloud Run 05 — 2026-04-30

## Purpose

Consolidate the technical evidence created so far into a conservative, reviewer-readable evidence package.

This run should not add new assistant behavior. It should document the implemented deterministic evaluation architecture, evidence value, limitations, and next steps in a way that supports future Global Talent Visa evidence preparation without overstating maturity.

## Current Repository Context

The `develop` branch already contains:

- Python package baseline
- deterministic response contract and guardrails
- emissions calculation helpers
- local knowledge-base loader/search
- deterministic assistant orchestrator
- evaluation scenario families
- evaluation summary reporting
- evaluation snapshot save/load helpers
- evaluation diff tooling and CLI comparison command
- test-only CI

Related ecosystem context:

- CarbonFactor-Parser exists as a separate repository and is being developed as deterministic carbon-factor parsing/data tooling.
- Do not inspect or claim implementation details from external repositories unless they are explicitly documented in this repository.
- You may mention CarbonFactor-Parser as a planned/adjacent integration point only in conservative language.

## Global Constraints

### Allowed

- Add evidence-oriented Markdown documentation.
- Add architecture/status documentation.
- Add a local evidence index.
- Add a short run-history summary based on existing files and docs in this repository.
- Update README only if it improves navigation.
- Add tests only if documentation links or simple metadata validation already exists or is trivial.

### Forbidden

Do not:

- add new assistant behavior
- add LLM SDK dependencies
- call external APIs
- add vector database or embedding dependencies
- add pandas or heavy data dependencies
- add database dependencies
- add cloud infrastructure
- add deploy/publish automation
- add PyPI publishing
- add Docker registry workflows
- add real secrets, tokens, or API keys
- claim production readiness
- claim regulatory compliance
- claim certification
- claim complete emissions accounting coverage
- claim market adoption, user adoption, or external validation
- claim official provider compatibility
- add a web UI

## Codex Cloud Branch Rule

The Codex Cloud environment may expose the checkout as a local branch named `work` and may not expose a normal Git remote. Treat the Codex Cloud environment branch selected in the UI as the source of truth.

Before making implementation changes:

1. Confirm this run plan file exists in the checkout.
2. Confirm the task IDs are COA-019 through COA-021.
3. Do not execute any earlier run plan.
4. Do not modify `main`.
5. Use the Codex Cloud PR workflow.
6. Final PR target must be `develop`.

Suggested branch name if branch creation is available:

```text
feature/codex-coa-005-evidence-consolidation
```

Suggested PR title:

```text
COA Cloud Run 05 — Add evidence consolidation docs
```

## Validation Rules

After each task:

```bash
python -m pytest -q
git status --short
```

If tests fail:

1. Fix only if the failure is clearly caused by the current task.
2. Re-run tests.
3. If still failing, stop and report.
4. Do not continue to the next task.

Before final PR:

```bash
python -m pytest -q
grep -RniE "production-ready|enterprise-grade|certified|compliant|fully automated|regulatory approved|official provider|market adoption|user adoption|docker login|pypi|secret|token|password" README.md docs .github src tests examples pyproject.toml || true
git status --short
git log --oneline -10
```

Risky language is acceptable only when used in non-goals, limitations, tests, sample questions, guardrails, or forbidden-scope documentation.

## Commit Rules

Use small commits if the Codex Cloud workflow supports it. Do not squash intentionally.

Suggested commits:

```text
COA-019 Add technical evidence index
COA-020 Add evaluation architecture summary
COA-021 Align README and evidence roadmap
```

Do not commit broken tests.

---

# Task Queue

## COA-019 — Technical Evidence Index

### Objective

Add a conservative index of implemented technical evidence in this repository.

### Required Changes

Create:

```text
docs/evidence/index.md
```

Update if useful:

```text
README.md
```

### Content Requirements

The evidence index should summarize implemented work under headings such as:

- deterministic assistant foundation
- response contract and guardrails
- emissions helper baseline
- evaluation scenario families
- evaluation summary reporting
- snapshot/diff tooling
- CLI commands
- testing and CI
- limitations and non-goals

For each evidence item include:

- implemented artifact/file references
- why it matters technically
- GTV relevance, stated conservatively
- current limitation
- next improvement candidate

Do not claim external impact, user adoption, or production usage.

### Completion Criteria

- evidence index exists
- language is conservative
- `python -m pytest -q` passes
- commit created

---

## COA-020 — Evaluation Architecture Summary

### Objective

Document how deterministic evaluation currently works end-to-end.

### Required Changes

Create:

```text
docs/evaluation/architecture.md
```

### Content Requirements

Explain the current local evaluation flow:

```text
scenario JSON files
  -> case loading/validation
  -> deterministic guardrail/status evaluation
  -> summary report
  -> JSON snapshot
  -> snapshot diff
  -> CLI commands
```

Include:

- current modules and responsibilities
- data contracts at a high level
- CLI commands
- what is measured
- what is not measured
- why no LLM/model-quality claims are made yet
- planned extension points

### Completion Criteria

- architecture summary exists
- no inflated claims
- `python -m pytest -q` passes
- commit created

---

## COA-021 — README and Roadmap Alignment

### Objective

Update repository navigation so a reviewer can find evidence and evaluation architecture quickly.

### Required Changes

Update:

```text
README.md
docs/roadmap.md
docs/evidence-backlog.md
```

### Documentation Requirements

- Link to `docs/evidence/index.md` from README.
- Link to `docs/evaluation/architecture.md` from README or evaluation docs.
- Update roadmap to reflect evidence consolidation as implemented.
- Keep future items conservative.
- Do not add production, compliance, certification, or adoption claims.

### Completion Criteria

- README navigation improved
- roadmap/backlog match implemented state
- `python -m pytest -q` passes
- commit created

---

## Final Report Required

At the end, report exactly:

```text
A. Summary
B. Files changed
C. Tests run and result
D. Safety scan result
E. Commits created with hashes
F. Pull request result, including PR target branch
G. Remaining risks
H. Recommended next task
```
